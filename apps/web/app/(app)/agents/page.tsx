import { Bot, CheckCircle2, Clock3, Radar } from "lucide-react";
import { Badge, Panel } from "@/components/ui";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AgentsPage(){const client=await createSupabaseServerClient();const {data:agents}=client?await client.from("agent_definitions").select("*").order("id"):{data:[]};return <div className="mx-auto max-w-[1200px] space-y-7"><div><div className="font-mono text-[10px] uppercase tracking-[.22em] text-teal">control plane / agents</div><h1 className="mt-2 font-display text-5xl leading-none">The team behind the trace.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Each agent has a narrow contract. The worker only runs capabilities represented in the persisted plan.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{(agents??[]).map((agent:{id:string;name:string;description:string;capabilities:string[]})=><Panel key={agent.id} className="p-5"><div className="flex items-start justify-between"><div className="grid h-9 w-9 place-items-center rounded-lg border border-teal/20 bg-teal/10 text-teal"><Bot size={17}/></div><Badge tone="teal">enabled</Badge></div><h2 className="mt-6 text-sm font-semibold">{agent.name}</h2><p className="mt-2 min-h-[48px] text-xs leading-5 text-muted">{agent.description}</p><div className="mt-5 flex flex-wrap gap-1.5">{(agent.capabilities??[]).map(cap=><span key={cap} className="rounded-md border border-line bg-ink px-2 py-1 font-mono text-[9px] uppercase tracking-[.12em] text-muted">{cap}</span>)}</div><div className="mt-5 grid grid-cols-3 gap-2 border-t border-line pt-4 text-center"><div><CheckCircle2 size={14} className="mx-auto text-teal"/><div className="mt-2 font-mono text-[9px] text-muted">persisted</div></div><div><Clock3 size={14} className="mx-auto text-amber"/><div className="mt-2 font-mono text-[9px] text-muted">bounded</div></div><div><Radar size={14} className="mx-auto text-ice"/><div className="mt-2 font-mono text-[9px] text-muted">scoped</div></div></div></Panel>)}</div></div>}
