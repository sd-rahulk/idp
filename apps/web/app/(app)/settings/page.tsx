"use client";
import { useState } from "react";
import { Check, KeyRound, LogOut, UserRound } from "lucide-react";
import { createSupabaseBrowserClient } from "@/lib/supabase/browser";
import { Button, Panel } from "@/components/ui";
export default function SettingsPage(){const [message,setMessage]=useState("");async function logout(){const client=createSupabaseBrowserClient();await client.auth.signOut();window.location.href="/";}return <div className="mx-auto max-w-[900px] space-y-7"><div><div className="font-mono text-[10px] uppercase tracking-[.22em] text-teal">workspace / settings</div><h1 className="mt-2 font-display text-5xl leading-none">Keep the boundary clear.</h1><p className="mt-3 text-sm text-muted">Account controls and project-level safeguards.</p></div><Panel className="p-6"><div className="flex items-center gap-3"><UserRound className="text-teal" size={19}/><div><h2 className="text-sm font-semibold">Account profile</h2><p className="text-xs text-muted">Profile details are managed by Supabase Auth.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold">Display name<input defaultValue="Rahul Welh" className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm"/></label><label className="text-xs font-semibold">Workspace<input defaultValue="Research workspace" className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm"/></label></div><div className="mt-5 flex justify-end"><Button onClick={()=>setMessage("Profile settings are read-only until the next milestone.")}><Check size={15}/> Save preference</Button></div>{message&&<div className="mt-4 text-right text-xs text-teal">{message}</div>}</Panel><Panel className="p-6"><div className="flex items-center gap-3"><KeyRound className="text-amber" size={19}/><div><h2 className="text-sm font-semibold">Security posture</h2><p className="text-xs text-muted">Live scans require verified targets and explicit scope.</p></div></div><div className="mt-6 space-y-3 text-sm text-muted"><div className="flex items-center justify-between rounded-lg border border-line bg-ink/50 px-4 py-3"><span>Source artifacts execute in worker</span><span className="font-mono text-[10px] text-teal">never</span></div><div className="flex items-center justify-between rounded-lg border border-line bg-ink/50 px-4 py-3"><span>LLM credentials in browser</span><span className="font-mono text-[10px] text-teal">never</span></div></div></Panel><Button variant="danger" onClick={logout}><LogOut size={15}/> Sign out</Button></div>}
