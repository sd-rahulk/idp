"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { ArrowRight, Loader2 } from "lucide-react";
import { createSupabaseBrowserClient } from "@/lib/supabase/browser";
import { Button, Panel } from "@/components/ui";

export default function SignupPage(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const client=createSupabaseBrowserClient();const {error}=await client.auth.signUp({email,password,options:{data:{display_name:name},emailRedirectTo:`${window.location.origin}/auth/callback`}});if(error)throw error;setMessage("Check your inbox to confirm the account, then sign in.");}catch(err){setError(err instanceof Error?err.message:"Unable to create account");}finally{setLoading(false);}}return <Panel className="p-7"><div className="mb-8"><div className="font-mono text-[10px] uppercase tracking-[.2em] text-teal">new workspace</div><h1 className="mt-3 font-display text-4xl">Start with proof.</h1><p className="mt-2 text-sm text-muted">Create a private security workspace for your owned projects.</p></div><form onSubmit={submit} className="space-y-4"><label className="block text-xs font-semibold">Name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm outline-none focus:border-teal" /></label><label className="block text-xs font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm outline-none focus:border-teal" /></label><label className="block text-xs font-semibold">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm outline-none focus:border-teal" /></label>{error&&<div className="rounded-lg border border-coral/30 bg-coral/10 px-3 py-2 text-xs text-coral">{error}</div>}{message&&<div className="rounded-lg border border-teal/30 bg-teal/10 px-3 py-2 text-xs text-teal">{message}</div>}<Button disabled={loading} className="w-full">{loading?<Loader2 className="animate-spin" size={15}/>:<ArrowRight size={15}/>}Create workspace</Button></form><div className="mt-6 text-center text-xs text-muted">Already registered? <Link href="/login" className="text-teal hover:underline">Sign in</Link></div></Panel>}
