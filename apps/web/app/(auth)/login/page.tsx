"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { ArrowRight, Loader2 } from "lucide-react";
import { createSupabaseBrowserClient } from "@/lib/supabase/browser";
import { Button, Panel } from "@/components/ui";

export default function LoginPage() { const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const client=createSupabaseBrowserClient();const {error}=await client.auth.signInWithPassword({email,password});if(error)throw error;window.location.href="/dashboard";}catch(err){setError(err instanceof Error?err.message:"Unable to sign in");}finally{setLoading(false);}} return <Panel className="p-7"><div className="mb-8"><div className="font-mono text-[10px] uppercase tracking-[.2em] text-teal">secure sign in</div><h1 className="mt-3 font-display text-4xl">Welcome back.</h1><p className="mt-2 text-sm text-muted">Continue where your assessment left off.</p></div><form onSubmit={submit} className="space-y-4"><label className="block text-xs font-semibold text-paper">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm outline-none focus:border-teal" /></label><label className="block text-xs font-semibold text-paper">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-lg border border-line bg-ink px-3 py-2.5 text-sm outline-none focus:border-teal" /></label>{error&&<div className="rounded-lg border border-coral/30 bg-coral/10 px-3 py-2 text-xs text-coral">{error}</div>}<Button disabled={loading} className="w-full">{loading?<Loader2 className="animate-spin" size={15}/>:<ArrowRight size={15}/>}Sign in</Button></form><div className="mt-6 flex justify-between text-xs text-muted"><Link href="/forgot-password" className="hover:text-teal">Forgot password?</Link><Link href="/signup" className="hover:text-teal">Create an account</Link></div></Panel>; }
