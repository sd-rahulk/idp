# Intentional vulnerable fixture

This folder is a safe, non-production fixture owned by the project author. It is designed only to exercise AegisFlow's deterministic source checks. It must never be deployed or used with real credentials.
